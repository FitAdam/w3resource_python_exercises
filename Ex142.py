"""142. Write a Python program to find the operating system name,
 platform and platform release date."""

import platform

print(platform.system())
print(platform.version())
print(platform.platform())